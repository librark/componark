import { Component } from '../component'

export class Paginator extends Component {

  init(context?: {
    collectionSize: number,
    pageSize?: number,
    currentPage?: number,
  }): Paginator
}

